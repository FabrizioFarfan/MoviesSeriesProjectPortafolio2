<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".ui.di.app.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoviesSeriesProject"
        tools:targetApi="31">
        <activity
            android:name=".ui.view.artist.ArtistActivity"
            android:exported="false"
            android:label="Artists"/>
        <activity
            android:name=".ui.view.tvshow.TvShowActivity"
            android:exported="false"
            android:label="Tv Shows"/>
        <activity
            android:name=".ui.view.movie.MovieActivity"
            android:exported="false"
            android:label="Movies"
          />
        <activity
            android:name=".ui.view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>